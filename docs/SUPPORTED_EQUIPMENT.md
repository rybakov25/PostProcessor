# Поддерживаемое оборудование

> **Универсальный постпроцессор** — поддерживает любое оборудование через систему конфигураций и Python-макросов

---

## 📋 Оглавление

1. [Обзор](#обзор)
2. [Поддерживаемые контроллеры](#поддерживаемые-контроллеры)
3. [Типы станков](#типы-станков)
4. [Добавление нового оборудования](#добавление-нового-оборудования)
5. [Примеры конфигураций](#примеры-конфигураций)
6. [Готовые профили](#готовые-профили)

---

## Обзор

Постпроцессор использует **двухуровневую систему конфигурации**:

| Уровень | Описание | Пример |
|---------|----------|--------|
| **Контроллер** | Тип ЧПУ, форматы координат, G/M-коды | Siemens 840D, Fanuc 31i |
| **Станок** | Кинематика, ограничения осей, опции | DMG Mori DMU50, Mori Seiki NLX2500 |

**Преимущества:**
- ✅ Одна конфигурация контроллера работает с несколькими станками
- ✅ Быстрое добавление поддержки нового оборудования
- ✅ Гибкая кастомизация через Python-макросы

---

## Поддерживаемые контроллеры

### Готовые конфигурации

| Контроллер | Семейство | Статус | Конфигурация | Макросы |
|------------|-----------|--------|--------------|---------|
| **Siemens 840D** | 840D / 840D sl | ✅ Готов | `siemens/840d.json` | ✅ base/, mmill/ |
| **Fanuc** | 31i / 32i / 35i | ✅ Готов | `fanuc/31i.json` | ✅ fanuc/ (фрезерные + токарные) |
| **Heidenhain** | TNC 640 / TNC 620 | ✅ Готов | `heidenhain/tnc640.json` | ✅ heidenhain/ |
| **Haas** | NGC / Next Gen | ✅ Готов | `haas/ngc.json` | ✅ haas/ |
| **Fagor** | CNC 8055 / 8065 | 📝 Планируется | — | — |
| **Mitsubishi** | M70 / E70 | 📝 Планируется | — | — |
| **Okuma** | OSP-P300 / OSP-L300 | 📝 Планируется | — | — |

### Добавление контроллера

**Шаг 1:** Создайте директорию:
```bash
mkdir configs/controllers/{controller_name}
```

**Шаг 2:** Создайте базовую конфигурацию `base.json`:
```json
{
  "name": "Your Controller Name",
  "machineType": "Milling",
  "description": "Custom controller",
  "registerFormats": {
    "X": { "address": "X", "format": "F4.3", "isModal": true },
    "Y": { "address": "Y", "format": "F4.3", "isModal": true },
    "Z": { "address": "Z", "format": "F4.3", "isModal": true },
    "F": { "address": "F", "format": "F3.1", "isModal": false },
    "S": { "address": "S", "format": "F0.0", "isModal": false }
  },
  "functionCodes": {
    "rapid": { "code": "G00" },
    "linear": { "code": "G01" },
    "spindle_cw": { "code": "M03" },
    "spindle_ccw": { "code": "M04" },
    "spindle_stop": { "code": "M05" },
    "coolant_on": { "code": "M08" },
    "coolant_off": { "code": "M09" }
  },
  "safety": {
    "clearancePlane": 100.0,
    "retractPlane": 5.0,
    "maxFeedRate": 10000.0
  }
}
```

**Шаг 3:** Добавьте базовые макросы:
```bash
mkdir macros/python/base
# Создайте: partno.py, goto.py, rapid.py, spindl.py, coolnt.py, fedrat.py, fini.py
```

**Шаг 4:** Используйте:
```bash
dotnet run -- -i part.apt -o part.nc -c your_controller
```

---

## Типы станков

### Фрезерные станки

| Тип | Осей | Примеры | Особенности |
|-----|------|---------|-------------|
| **3-осевые** | X, Y, Z | DMG Mori DMU50, Mazak VCN | Базовая поддержка |
| **5-осевые** | X, Y, Z, A/B, C | DMG Mori DMU50 5-axis, Haas UMC | RTCP, CYCLE800 |
| **Портальные** | X, Y, Z | Gantry-станки | Синхронизация осей |

### Токарные станки

| Тип | Осей | Примеры | Особенности |
|-----|------|---------|-------------|
| **2-осевые** | X, Z | Mori Seiki NLX2500 | Простая токарка |
| **Токарные центры** | X, Z, C, Y | Mazak Integrex | Приводной инструмент |
| **Многошпиндельные** | X, Z, C, Y, B | Swiss-станки | Несколько инструментов |

### Многозадачные станки (Mill-Turn)

| Тип | Осей | Примеры | Особенности |
|-----|------|---------|-------------|
| **5-осевые** | X, Y, Z, B, C | Mazak Integrex i-400 | Фрезеровка + токарка |
| **7-осевые** | X, Y, Z, B, C + Y2 | Okuma Multus | Два суппорта |

---

## Добавление нового оборудования

### Шаг 1: Определите тип оборудования

Ответьте на вопросы:
1. **Тип станка?** Фрезерный / Токарный / Многозадачный
2. **Количество осей?** 3 / 4 / 5 / больше
3. **Тип контроллера?** Siemens / Fanuc / Heidenhain / другой
4. **Специфичные функции?** RTCP / CYCLE800 / синхронизация

### Шаг 2: Создайте профиль станка

**Файл:** `configs/machines/{machine_name}.json`

```json
{
  "name": "DMG Mori DMU50 5-Axis",
  "machineProfile": "dmu50_5axis",
  "machineType": "Milling",
  "axes": 5,
  
  "axisLimits": {
    "X": { "min": -650, "max": 650 },
    "Y": { "min": -650, "max": 650 },
    "Z": { "min": -460, "max": 650 },
    "A": { "min": -30, "max": 110 },
    "C": { "min": 0, "max": 360 }
  },
  
  "fiveAxis": {
    "enableRtcp": true,
    "rtcp": {
      "on": "TCPM",
      "off": "TCPOF"
    },
    "orientationType": "ABC"
  },
  
  "toolChanger": {
    "maxTools": 30,
    "toolChangeTime": 2.5
  },
  
  "customParameters": {
    "useCustomCycle": true
  }
}
```

### Шаг 3: Добавьте специфичные макросы

**Структура:**
```
macros/python/
├── base/                    # Базовые макросы (универсальные)
├── fanuc/                   # Fanuc 31i/32i/35i (фрезерные + токарные)
├── heidenhain/              # Heidenhain TNC640/TNC620
├── haas/                    # Haas NGC
├── lathe/                   # Токарные макросы (TURRET, CHUCK, TAILSTK)
├── mmill/                   # Mecof MMILL
└── user/                    # Пользовательские переопределения
    └── {machine}/           # Переопределения для станка
```

**Доступные макросы для каждого контроллера:**

| Макрос | Fanuc | Heidenhain | Haas | Описание |
|--------|-------|------------|------|----------|
| `init.py` | ✅ | ✅ | ✅ | Инициализация переменных |
| `partno.py` | ✅ | ✅ | ✅ | Начало программы |
| `fini.py` | ✅ | ✅ | ✅ | Завершение программы |
| `goto.py` | ✅ | ✅ | ✅ | Линейное перемещение |
| `rapid.py` | ✅ | ✅ | ✅ | Быстрое перемещение |
| `spindl.py` | ✅ | ✅ | ✅ | Управление шпинделем |
| `coolnt.py` | ✅ | ✅ | ✅ | Управление охлаждением |
| `fedrat.py` | ✅ | ✅ | ✅ | Управление подачей |
| `loadtl.py` | ✅ | ✅ | ✅ | Смена инструмента |
| `turret.py` | ✅ (lathe) | ❌ | ❌ | Револьверная головка |
| `chuck.py` | ✅ (lathe) | ❌ | ❌ | Управление патроном |
| `tailstk.py` | ✅ (lathe) | ❌ | ❌ | Управление пинолью |

**Пример макроса для 5-осевого станка:**
```python
# -*- coding: ascii -*-
# 5AXMOVE - 5-осевое перемещение

import math

def execute(context, command):
    if not command.numeric:
        return
    
    # Получение координат
    x = command.getNumeric(0, context.registers.x)
    y = command.getNumeric(1, context.registers.y)
    z = command.getNumeric(2, context.registers.z)
    
    # Получение углов (IJK → ABC)
    i = command.getNumeric(3, 0.0)
    j = command.getNumeric(4, 0.0)
    k = command.getNumeric(5, 1.0)
    
    # Конвертация IJK в ABC углы
    a = math.degrees(math.atan2(j, k))
    b = math.degrees(math.atan2(-i, math.sqrt(j*j + k*k)))
    c = 0.0
    
    # Обновление регистров
    context.registers.x = x
    context.registers.y = y
    context.registers.z = z
    
    # Вывод с RTCP
    if context.globalVars.GetDouble("RTCP_ACTIVE", 0.0) == 1.0:
        context.write(f"TCPM X{x:.3f} Y{y:.3f} Z{z:.3f} A{a:.3f} B{b:.3f} C{c:.3f}")
    else:
        context.write(f"G1 X{x:.3f} Y{y:.3f} Z{z:.3f}")
```

### Шаг 4: Протестируйте

```bash
# Запуск с новым профилем
dotnet run -- -i test.apt -o output.nc \
  -c siemens \
  --machine-profile dmu50_5axis \
  --debug

# Проверка вывода
cat output.nc
```

---

## Примеры конфигураций

### 3-осевой фрезерный станок (Siemens 840D)

**Команда:**
```bash
dotnet run -- -i part.apt -o part.nc \
  -c siemens \
  --machine-profile dmu50_3axis
```

**Профиль:** `configs/machines/dmu50_3axis.json`
```json
{
  "name": "DMG Mori DMU50 3-Axis",
  "machineProfile": "dmu50_3axis",
  "axes": 3,
  "axisLimits": {
    "X": { "min": -650, "max": 650 },
    "Y": { "min": -650, "max": 650 },
    "Z": { "min": -460, "max": 650 }
  }
}
```

### 5-осевой обрабатывающий центр (Siemens 840D sl)

**Команда:**
```bash
dotnet run -- -i impeller.apt -o impeller.nc \
  -c siemens \
  --machine-profile dmu50_5axis
```

**Профиль:** `configs/machines/dmu50_5axis.json`
```json
{
  "name": "DMG Mori DMU50 5-Axis",
  "machineProfile": "dmu50_5axis",
  "axes": 5,
  "axisLimits": {
    "X": { "min": -650, "max": 650 },
    "Y": { "min": -650, "max": 650 },
    "Z": { "min": -460, "max": 650 },
    "A": { "min": -30, "max": 110 },
    "C": { "min": 0, "max": 360 }
  },
  "fiveAxis": {
    "enableRtcp": true,
    "rtcp": { "on": "TCPM", "off": "TCPOF" }
  }
}
```

### Токарный станок (Fanuc 31i)

**Команда:**
```bash
dotnet run -- -i shaft.apt -o shaft.nc \
  -c fanuc \
  --machine-profile nlx2500_01
```

**Профиль:** `configs/machines/nlx2500_01.json`
```json
{
  "name": "Mori Seiki NLX2500",
  "machineProfile": "nlx2500_01",
  "machineType": "Turning",
  "axes": 2,
  "axisLimits": {
    "X": { "min": 0, "max": 300 },
    "Z": { "min": -500, "max": 50 }
  },
  "turret": {
    "stations": 12,
    "type": "VDI40"
  }
}
```

---

## Готовые профили

### В репозитории

| Профиль | Тип | Контроллер | Файл |
|---------|-----|------------|------|
| MMILL | 3-осевой фрезерный | Siemens 840D | `machines/mmill.json` |
| DMU50_3AXIS | 3-осевой фрезерный | Siemens 840D | `machines/dmu50_3axis.json` |
| DMU50_5AXIS | 5-осевой фрезерный | Siemens 840D sl | `machines/dmu50_5axis.json` |
| NLX2500_01 | Токарный | Fanuc 31i | `machines/nlx2500_01.json` |

### Создание своего профиля

1. Скопируйте существующий профиль:
```bash
cp configs/machines/dmu50_3axis.json configs/machines/my_machine.json
```

2. Отредактируйте параметры:
   - `name` — название станка
   - `machineProfile` — идентификатор
   - `axisLimits` — ограничения осей
   - `fiveAxis` — настройки 5-осевой обработки

3. Используйте:
```bash
dotnet run -- -i part.apt -o part.nc --machine-profile my_machine
```

---

## Таблица совместимости

| Контроллер | 3-ось | 5-ось | Токарный | Mill-Turn |
|------------|-------|-------|----------|-----------|
| Siemens 840D | ✅ | ✅ | ✅ | ✅ |
| Fanuc 31i | ✅ | ✅ | ✅ | ✅ |
| Heidenhain TNC640 | ✅ | ✅ | ❌ | ❌ |
| Haas NGC | ✅ | ✅ | ✅ | ⚠️ |
| Fagor 8065 | ✅ | ⚠️ | ✅ | ❌ |

**Обозначения:**
- ✅ — Полная поддержка
- ⚠️ — Частичная поддержка / Требуется настройка
- ❌ — Не поддерживается / Требуется разработка

---

## Дополнительные ресурсы

- [CUSTOMIZATION_GUIDE.md](CUSTOMIZATION_GUIDE.md) — Руководство по настройке
- [PYTHON_MACROS_GUIDE.md](PYTHON_MACROS_GUIDE.md) — Создание макросов
- [ARCHITECTURE.md](ARCHITECTURE.md) — Архитектура постпроцессора
- [QUICKSTART.md](QUICKSTART.md) — Быстрый старт
